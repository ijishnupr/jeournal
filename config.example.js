// Local development only: copy to config.js (gitignored) and fill in.
// In production the deploy workflow writes config.js from the repository
// variables (Settings → Secrets and variables → Actions → Variables).
window.FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: '',
};

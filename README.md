# jeournal

## Preview locally

```
./preview.sh
```

Then open http://localhost:8080 (sign in, your real data) or
http://localhost:8080/?demo (sample data, no sign-in, nothing saved).
Edit `index.html` and reload — no build step.
